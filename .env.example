# -----------------------------------------------------------------------------
# PostgreSQL Database Connection
# -----------------------------------------------------------------------------
# Hostname or IP address of your PostgreSQL server.
PGHOST=localhost

# Port your PostgreSQL server is running on (default is 5432).
PGPORT=5432

# The name of the PostgreSQL database to connect to.
PGDATABASE=your_db_name

# The PostgreSQL username for authentication.
PGUSER=your_db_user

# The password for the PostgreSQL user.
# IMPORTANT: For production environments, use a more secure method for
# managing secrets, such as environment-specific files or a secrets
# management service.
PGPASSWORD=your_db_password


# -----------------------------------------------------------------------------
# Ollama Language Model Configuration
# -----------------------------------------------------------------------------
# The base URL of the Ollama server.
# For local installations, this is typically the default.
# Ensure this endpoint is trusted and secured.
OLLAMA_BASE_URL=http://127.0.0.1:11434

# The name of the Ollama model to use for generating SQL.
# Example: "llama3.2:latest", "codellama:latest"
# IMPORTANT: It is highly recommended to use the ALLOWED_OLLAMA_MODELS
# variable below to prevent the use of untrusted or untested models.
OLLAMA_MODEL=llama3.2:latest

# (Optional) A comma-separated list of trusted Ollama model names that the
# application is allowed to use. If this is set, the application will refuse
# to start if OLLAMA_MODEL is not in this list. This is a security measure
# to ensure only approved models are used.
# Example: "llama3.2,codellama"
ALLOWED_OLLAMA_MODELS=

# The timeout in seconds for requests to the Ollama server.
# Increase this if you are using a large model or have a slow connection.
OLLAMA_TIMEOUT=60


# -----------------------------------------------------------------------------
# Application Behavior
# -----------------------------------------------------------------------------
# The default maximum number of rows that a SQL query can return if no
# LIMIT clause is specified in the generated SQL. This is a safeguard to
# prevent accidental large queries from overwhelming the database.
DEFAULT_ROW_LIMIT=500

# The number of requests a user can make per minute. This helps prevent
# abuse and ensures the application remains available for other users.
RATE_LIMIT_PER_MINUTE=15
